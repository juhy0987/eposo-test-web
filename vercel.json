{
  "version": 2,
  "installCommand": "cd eposo-test-web && npm install",
  "buildCommand": "cd eposo-test-web && npm run build",
  "outputDirectory": "eposo-test-web/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}